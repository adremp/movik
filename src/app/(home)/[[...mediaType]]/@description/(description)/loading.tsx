import text from "@/shared/styles/text";

interface HomeDescLoadingProps {}

const HomeDescLoading = (props: HomeDescLoadingProps) => {
  return <div className={text({ size: "100" })}>HomeDescLoading</div>;
};

export default HomeDescLoading;
